SELECT min(orderdate) AS firstdate
FROM orders;

-- Need to be familiar with how to use the aggregate function
-- Make sure what the difference between WHERE clause and HAVING clause is: should be familiar with their uses later
